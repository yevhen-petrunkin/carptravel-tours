import { Container } from "@/components";

const Hero = () => {
  return (
    <section>
      <Container isHero background="bg-hero">
        Hero
      </Container>
    </section>
  );
};

export default Hero;
